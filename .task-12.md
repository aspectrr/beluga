# Task 12: Core Model Types, Config, Database Pool

## Files Created

### 1. `internal/core/model/types.go`

- Session struct with ID, Source, SourceID, Status, SandboxID, Metadata, timestamps
- Event struct with ID, SessionID, Seq, Type, Data, CreatedAt
- 5 status constants: pending, running, suspended, completed, failed
- 8 event type constants: user_message, agent_message, tool_call, tool_result, interrupt, status_transition, error, compacted
- 8 payload types: UserMessagePayload, AgentMessagePayload, ToolCallPayload, ToolResultPayload, InterruptPayload, StatusTransitionPayload, ErrorPayload, CompactedPayload

### 2. `internal/core/config/config.go`

- Config: LLM, Database, Workspace, Agent, Extensions (map[string]ExtensionConfig)
- LLMConfig with optional EmbeddingModel/EmbeddingDimensions
- DatabaseConfig with DSN() method
- WorkspaceConfig for Docker sandbox defaults
- AgentConfig with Defaults() method (max_iterations=30, max_context_tokens=128000)
- ExtensionConfig with Enabled bool and Config json.RawMessage
- LoadConfig() with YAML parsing and env var overrides (BELUGA*LLM*_, BELUGA*DB*_)
- Helper methods: IsExtensionEnabled(), ExtensionRawConfig(), EnabledExtensions()

### 3. `internal/core/database/pool.go`

- NewPool(ctx, DatabaseConfig) (\*pgxpool.Pool, error)
- Uses pgx/v5 connection pool
- Respects MaxConnections config
- Pings on connect, returns error on failure

## Validation

- `go mod tidy` completed successfully
- `go build ./internal/core/...` — all three packages compile cleanly
